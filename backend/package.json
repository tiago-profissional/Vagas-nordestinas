{
  "name": "backend-api",
  "version": "1.0.0",
  "description": "API JSON Server para Vagas Nordestinas",
  "scripts": {
    "start": "json-server db.json --port 3001",
    "dev": "json-server db.json --port 3001 --watch"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}